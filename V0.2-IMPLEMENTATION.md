# ゲームv0.2：日次進行の試作

この文書はv0.2時点の範囲です。現行の球団運営機能・新しい保存版は [v1.0への進捗](V1.0-PROGRESS.md) を参照してください。

v0.2の目標は、その日の全試合を処理し、結果・成績・順位を反映して翌日へ進めることです。今回の範囲は架空4球団、4日間、6試合と試合のない1日です。年間日程を完走するv1.0、資料自体のv1.0、試合計算のv10とは区別します。

## 操作と確認

1. `npm.cmd run dev` で起動し、「日程・球団運営」を開きます。
2. 「1日を自動進行」で当日の全試合を進めます。初期seedは20260924です。
3. 最初の日は星原0対青凪2、湖白2対朝霧4で終了します。現在日が2026-09-25へ進み、結果の日付は2026-09-24のままです。
4. 順位表と累計個人成績、日別の試合詳細を確認します。最初の試合終了だけでは日付は進みません。
5. ページを再読み込みし、「自動保存を読み込む」で再開します。読み込み後は停止状態です。
6. 9/26は試合のない日です。成績を変えずに日次確定・翌日への移行・自動保存を行います。9/27の試合を完了すると9/28で試作日程終了となります。

一時停止後の「世界を手動保存」は進行中試合、既に終了した他試合、日次cursor、集計、開始時定義、各試合の完全な乱数状態を一緒に保存します。自動保存1枠・直前自動1枠・手動1枠を用意し、自動保存で手動枠を上書きしません。

自動保存の失敗では、その日の確定結果を作業状態に残し、日付の公開を保留します。「日次確定を再試行」で保存をやり直せます。破損した最新保存は拒否し、直前自動または手動保存から復旧できます。別タブによる保存世代の変更も拒否します。

## 編集先

| 対象                             | ファイル                                                                 |
| -------------------------------- | ------------------------------------------------------------------------ |
| 試合日・対戦・試作期間           | [src/data/world/schedule.ts](src/data/world/schedule.ts)                 |
| 参加球団・追加球団のデータ組立   | [src/data/world/teams.ts](src/data/world/teams.ts)                       |
| 大会名・年度・集計区分・設定取込 | [src/data/world/index.ts](src/data/world/index.ts)                       |
| 基になる選手・持ち球・編成       | [データ編集ガイド](src/data/README.md)                                   |
| 試合の係数                       | [config/match.ts](config/match.ts)、[config/errors.ts](config/errors.ts) |

`npm.cmd run data:check` で入力を検査します。変更を反映するには「新しい日程を準備」を実行します。開始済み・保存済みの世界は当時の入力で再現します。現在のデータを補って過去の結果を変えません。

試作の入力上限は2〜8球団、期間31日以内、32試合以内、各球団1日1試合です。保存上限は1スナップショット64MiBです。上限到達を正常終了や自動削除へ置き換えません。

## 仮定と版

- 世界進行は `world-prototype-v1`、世界定義は `world-definitions-v1`、保存は `v02-world-snapshot-v1`。
- 試合は既存の `game-prototype-v10` を共通使用します。試合の係数・式は変更せず、未校正のままです。
- 湖白・朝霧の能力と持ち球は既存2球団を複製した動作確認用データです。選手・持ち球のIDは元の安定IDへ球団接頭辞を付けます。名前や配列順をIDにしません。
- 日程IDの昇順で当日計画を固定します。試合のseedはUTF-8文字列 `game-id-fnv1a32-v1:{worldSeed}:{gameId}:attempt:1` のFNV-1a 32bit（初期値2166136261、乗数16777619、uint32）です。結果0だけ1へ写します。試合順による乱数消費はなく、各試合にxorshift32-v1の完全な状態と消費数を保存します。世界生成・成長用乱数は未実装であり、空のストリームを完成済みとして作りません。
- 個人・球団・対戦別の整数カウンタを年度・大会・firstRegular・squadIdで区分します。投球回はアウト数を加算し、率は表示時に導出します。試合ID・attemptNo・resultRevision・寄与と適用済みマーカーで二重加算を防ぎます。結果訂正・寄与取り消しは未対応なので、異なる寄与の再適用を拒否します。
- 順位規則は `win-percentage-shared-rank-v1`。勝率=勝利/(勝利+敗戦)。分母0は未観測で、率・順位ともnullです。同率は同順位（1位・1位・3位）、同順位内の表示はsquadId順です。丸め前の分子・分母をBigIntの交差積で比較します。NPBの同率決定規則を実装済みとは扱いません。
- 契約・成長・回復・怪我・経済・行事・必須選択は未実装です。今回の日次処理は全試合集計、日付と処理済み情報の更新までです。試合を跨いだ疲労補正や投手ローテーションを加えず、各試合を開始時の能力・編成で行います。
- 二軍を含めるかは原資料で未決定です。今回のデータは一軍相当の架空1大会です。

## 保存と実装境界

`src/world/` のエンジン・集計はReact/DOM/DBから独立しています。世界用Workerが正本を持ち、UIは指示と表示を担当します。表示用コピーや画面速度は計算に影響しません。1回の進行指示は最大25イベントです。

IndexedDBの `BaseballSim-v02-worlds` をStorageAdapter経由で利用します。既存v0.1の `BaseballSim-v01-results` と名前空間を分け、旧保存を保持します。4物理ストアはlocal_worlds、save_slots、save_snapshots、save_blocksです。世界定義・世界状態・成績/寄与・各試合を別の不変ブロックに保存し、SHA-256と全イベントの再実行で検査します。FNVによる寄与識別子は暗号学的な完全性検査の代わりには使いません。

圧縮・履歴GC・ファイル入出力・クラウド同期・複数プレイ枠・複数手動枠・旧試合を世界へ取り込む移行は未対応です。保存履歴は蓄積します。長期シーズン向けの容量・性能を保証する保存形式ではありません。

## 検証

```powershell
npm.cmd run data:check
npm.cmd run day -- 20260924
npm.cmd run verify:v02
```

- [tests/world.test.ts](tests/world.test.ts)：日付の一度だけの更新、1/25イベント進行と試合順の再現、試合なし/月境界、成績の合算・寄与再送、率の正確な比較、入力拒否、途中世界の保存、4ストアのロールバック、世代競合、自動保存失敗後の再試行、破損拒否と復旧。
- [e2e/world.spec.ts](e2e/world.spec.ts)：1280px/390pxで日次進行・結果・順位・累計・再読込・直前自動・試作最終日までの操作、途中の一時停止/手動保存/再開。
- 既存v0.1の試験と凍結済み旧10モデルの保存・全記録ハッシュも継続して検査します。Android実機・他ブラウザ・年間進行の検証は未実施です。

根拠はローカル原資料の引き継ぎメモ3章（v0.2）、データ設計書v1.0の9章（集計）、13.1〜13.3（日次・再現性）、14章（保存）です。上記の暫定データ・順位規則・制限は実装上の試作採用であり、最終仕様の承認とは区別します。
