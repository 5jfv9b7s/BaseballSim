# v1.0へ向けた実装：球団運営

今回は「自球団を運営しながら少なくとも1シーズンを完走する」というv1.0目標のうち、試合前の起用設定を実装しました。実装版は0.3.0です。年間日程・育成・契約・経営は未完成で、ゲームv1.0の完成宣言ではありません。

## 使い方

1. `npm.cmd run dev`で起動し、「日程・球団運営」を開きます。
2. 最初は星原を担当します。球団を選ぶ場合は「新規日程・担当球団と試作の範囲」で担当球団とseedを選び、「新しい日程を準備」を押します。
3. 「打順・守備・投手起用を編集」を開き、上下ボタンで打順、選択欄で守備位置を変更します。同じ守備位置を重複指定したまま確定はできません。
4. 先発枠数・順番・次に使う枠、救援の優先順を設定し、「編成を確定して自動保存」を押します。
5. その日だけ先発を変更する場合は「今日の先発指定」を使います。「ローテーションに従う」を確定すると解除します。
6. 「1日を自動進行」で、確定した編成の試合を実行します。当日の最初の試合を始めた後は、翌日まで編成を編集できません。
7. 再読み込み後の「自動保存を読み込む」で設定・履歴・日程を復元できます。試合途中は一時停止して世界全体を手動保存できます。

設定の確定は保存成功後に行います。保存失敗では編集前の正本と以前の保存を維持し、画面の下書きから再試行できます。既存のv0.2保存も従来の規則で読み込み・進行できますが、編成操作は新しい担当球団プレイで利用してください。

## 今回の試作規則

- 世界モデルは`world-prototype-v2`、起用設定は`club-management-v1`、新保存は`v03-world-snapshot-v2`です。試合エンジンv10、式、係数は変更していません。
- データ設計書6.2のideal_lineupsとpitcher_usage_plansから、DH制の打順9人・守備9枠・先発ローテ・通常救援優先順を使う部分だけを型にしました。Pの理想配置はnullとし、当日の先発で実配置を確定します。打者のDH枠と投手枠は分けます。
- 先発枠は1〜6人、同じ投手は重複不可です。全投手を先発枠か通常救援へ重複なく割り当てます。0人のローテ・条件別救援役割・左右別オーダーは今回の対象外です。
- ローテの次枠は正常終了した実施試合につき一度だけ進みます。試合のない日、表示、保存、試合途中の停止では進みません。異常停止を実施済みとして扱いません。
- 当日代役は日程の試合IDに結び付け、元のローテ割当を変更しません。代役使用後も通常どおり次の枠へ進める試作規則です。
- 当日先発と指定された救援だけを試合名簿に含めます。他のローテ投手はベンチ外です。救援が0人なら先発が最後まで投げる既存の試作規則を使います。
- 疲労・登板間隔・適性による自動休養や先発選び直しは未計算です。先発6人を用意したことを疲労モデルの完成とは扱いません。CPU球団も同じ試合・起用処理を使いますが、能力や育成方針を見て理想オーダーを自動変更する判断は未実装です。
- 編集できるのは担当球団のみです。試合開始後の変更を防ぐため、今回は当日の最初の試合より前に編集を限定します。日程終了後の編集はできません。試合中の選手交代や代打は未対応です。
- 編成履歴は日付・順序・指示IDと内容を保存します。既定データへ過去の編成を上書きせず、保存内の初期定義に履歴を順に適用して試合を再現します。既に開始・終了した試合の名簿は後日の編集で変えません。
- 編成確定後のactionAutoは自動枠を更新し、以前の自動枠を直前自動へ移します。手動枠は変更しません。保存先DBと4物理ストアはv0.2と共通で、世界モデルと保存形式を対応させて検査します。

能力・係数校正は後工程です。今回の数値的な追加は編成上限（最大6先発枠、試作履歴256件）で、能力へ新しい補正係数は加えていません。4球団・4日・6試合の確認用日程は継続します。旧世界の上限（31日以内・32試合以内・1保存64MiB）も維持しています。

## ファイルと確認

| 場所                     | 役割                                           |
| ------------------------ | ---------------------------------------------- |
| src/world/management.ts  | 起用設定の検査、当日名簿、変更履歴、ローテ進行 |
| src/world/types.ts       | 理想オーダー・投手起用・新旧世界の型           |
| src/world/controller.ts  | 所有者検査、指示再送、保存成功後の確定         |
| src/world/validation.ts  | 編成履歴を含む全世界の再実行                   |
| src/ui/ClubEditor.tsx    | 打順・守備・投手運用の下書き編集               |
| tests/management.test.ts | 編成・ローテ・保存失敗・旧保存の検証           |
| e2e/management.spec.ts   | PC/スマートフォン幅の編集と保存再開            |

```powershell
npm.cmd run check
npm.cmd run test:ui
npm.cmd run format:check
```

変更前55c8b7eのv0.2実保存を`tests/fixtures/world-v1-save.json.gz`へ凍結しています。自動保存の全世界SHA-256は`1bd6f47fa568d3f96d7190f84f42183fc437ef4ef86689852c0eda050af8c79a`です。途中手動保存からの進行でもこの記録を再現します。期待値は新実装から再生成していません。

## v1.0へ残る工程

以下は次工程の整理案であり、細かなゲーム仕様の承認済み決定ではありません。

- 年間日程・月単位の移動・シーズン終了と年度要約。長期保存に合わせて容量と全履歴再実行のコストも検討する。
- 控えを含む名簿、一二軍・登録、状態に応じたCPU起用。
- 体力・疲労・成長・怪我等の必要範囲を決め、試作モデルと設定を追加する。
- 契約・育成・経営のv1.0必須範囲を確定する。
- 合意した範囲で1シーズンを通し、保存・再開・集計・終了処理を検証する。

根拠：原資料の引き継ぎメモ3章、データ設計書v1.0の6.2・13.1・14章、要件定義書FR-BASE-02・FR-ORD-01・FR-PUSE-05。資料版とゲームの完成段階は区別します。
