# 作業履歴

## 2026-09-23 / W-001 / 1球試作の初回実装

依頼：既存資料を保持した環境構築・型・架空最小データ・1球処理、仮定の明示、quiz_app方式。

実施：原AGENTS・引き継ぎメモ・現行4文書の対象章、quiz_appの管理指針を確認。ルートへアプリを追加し、knowledge方式を統合。正本をWorkerへ置き、エンジン・UI・データを分離。未解決分岐を明示したモデルv1、17区画、乱数完全状態、検査、CLIと画面を追加。原Docsは編集していない。

検証：`npm.cmd run check` の型検査・10件の試験・ビルドに成功。2,000seed条件を含む試験でJSON往復の負のゼロ問題を発見し修正。`npm.cmd run pitch -- 20260923` に相当するCLI実行で空振り・146.59km/hを確認。`npm.cmd run test:ui` でEdgeの1280px/390px幅2件成功。ブラウザ接続スキルの起動は環境エラーのため、Playwrightへ切り替えた。

制約：統計校正・1打席の正式完了・1試合・成績・保存・Android実機は未対応。次はcurrent-stateの順で打席・走者を解決する。段階ごとの承認待ちは導入していない。

最終確認：原資料6ファイルのSHA-256は作業前後で全て一致。PC幅・スマートフォン幅の画面PNGを目視確認。制限環境ではPlaywrightのサーバー終了待ちが残ったが、許可を得て検証プロセスを終了し、制限外で再実行して `2 passed`・終了コード0を確認した。型検査もブラウザ試験コードを含めて成功。

## 2026-09-23 / W-002 / GitHub接続

依頼：ユーザー指定の5jfv9b7s/BaseballSimへ接続。Gitをmainで初期化し、originを登録。git ls-remoteで接続先に既存コミットがないことを確認。実行環境による所有者の違いは、許可を得てこのフォルダだけをsafe.directoryへ登録して解決した。

同期対象：ソース・テスト・構成・ロックファイル・README・knowledge。ユーザーが追記していたAGENTS.md・Docs/の除外を維持し、追加依頼に従ってカバレッジ、Playwright追加レポート・認証一時情報、ローカル設定、OS生成物の除外を補った。git remote -vとgit check-ignoreで接続先と除外を確認。ゲームコードは変更していないため、前回成功した型・エンジン・画面試験は再実行していない。

同期の確認方法：git status --short --branch、git rev-parse HEAD、git ls-remote origin refs/heads/mainでローカルとリモートのコミットを比較する。

GitHub送信状況：originの接続確認とローカル初回コミットの準備まで完了。mainへのpushは自動承認レビューが、ソース・設定・knowledgeを含む30ファイルの外部送信について明示承認を求めたため未実施。ユーザーの送信承認後、git push -u origin mainとコミット一致確認を行う。
