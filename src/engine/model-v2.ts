import { MODEL } from './model.ts';

/** 未校正。能力→判断→接触を調整し、打席結果を直接抽選しない。 */
export const PITCH_MODEL_V2 = Object.freeze({
  ...MODEL,
  version: 'pitch-prototype-v2',
  schemaId: 'pitch-prototype-config-v2',
  rulesetVersion: 'game-rules-prototype-v3',
  aimXMm: [-190, 0, 190] as const,
  aimZMm: [550, 800, 1050] as const,
  chaseAimXMm: [-245, 0, 245] as const,
  chaseAimZMm: [460, 800, 1140] as const,
  threeBallAimXMm: [-144, 0, 144] as const,
  threeBallAimZMm: [600, 800, 1000] as const,
  swingInBase: 0.36,
  swingInAggression: 0.3,
  swingInDiscipline: 0.12,
  swingOutBase: 0.24,
  swingOutAggression: 0.2,
  swingOutDiscipline: 0.24,
  protectInside: 0.2,
  protectOutside: 0.06,
  threeBallPatience: 0.12,
  contactBase: 0.48,
  contactAbility: 0.46,
  foulBase: 0.5,
  foulOutside: 0.1,
  foulTwoStrike: 0.03,
  bodyCenterMm: 410,
  bodyHalfWidthMm: 75,
  bodyBottomMm: 400,
  bodyTopMm: 1500,
  deliveryDistanceMeters: 18.44,
  avoidanceReactionSeconds: 0.18,
  avoidanceDisciplineReductionSeconds: 0.06,
  avoidanceSurpriseSeconds: 0.28,
  avoidanceSpeedMetersPerSecond: 0.9,
  avoidanceMaxShiftMm: 220,
} as const);
